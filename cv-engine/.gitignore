# Local secrets and generated data
config.yaml
output/
*.pdf
# NOTE: data/profile.yaml IS committed (career content only). Personal contact
# details are injected at runtime via CV_CONTACT_* env vars, never committed.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/
